/**
 * Booking Service Skeleton
 */
export const bookingService = {
  createBooking: async (userId: string, data: any) => {},
  getUserBookings: async (userId: string) => {},
  getBookingById: async (id: string, userId?: string) => {},
  cancelBooking: async (id: string, userId: string, reason: string) => {},
};
