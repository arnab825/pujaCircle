import React from 'react';

/**
 * AuthModal Skeleton
 * Reserved for future mobile OTP dialog flows.
 * Primary authentication for development is currently handled via /auth/login.
 */
export const AuthModal: React.FC = () => {
  return null;
};

export default AuthModal;
